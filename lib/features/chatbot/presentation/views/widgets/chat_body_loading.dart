import 'package:flutter/material.dart';

class ChatBodyLoading extends StatelessWidget {
  const ChatBodyLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}